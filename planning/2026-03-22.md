# 2026-03-22 開發紀錄

## 今日完成項目

---

### 1. Admin Console 分頁化（Tab Navigation）

**問題：** Admin 頁面資料量龐大，需要一直滾動才能到達目標區塊，操作不方便。

**改動：**
- 新增固定頂部 Navbar（`position: fixed`、backdrop blur 毛玻璃效果）
- 將 Admin Console 拆分為 6 個分頁：
  - **總覽** — Hero stats、系統狀態、Metrics、系統說明
  - **控制** — Pipeline、Scheduler、Kill Switch
  - **市場資料** — 資料狀態、手動抓取、Feature Store（新分頁）
  - **監控** — Strategy Activity、Queue、Heartbeats、Orders、Positions、Trades、Portfolio、Risk Config
  - **ML / AI** — Training Jobs、Model Registry、Inference、RL Experiment
  - **診斷** — Health Report、PnL、Pipeline Result、Audit、Alerts、Soak Validation
- Feature Store 從 ML/AI 分頁移至市場資料分頁（概念上更符合資料層）

**檔案：** `app/admin/page.py`

---

### 2. 市場資料層改善

#### 2a. `GET /candles/status` — 新增 Candle 狀態 API

**問題：** 沒有任何 API 可以查看每個 symbol 的 candle 數量、資料新鮮度與缺口狀況。

**改動：** `app/data/candles_service.py` 新增 `get_candles_status()`，回傳每個 `(symbol, timeframe)` 的：
- `count` — candle 總數
- `earliest` / `latest` — 最早與最新時間（UTC ISO 格式）
- `stale_seconds` — 距最新一根 candle 的秒數
- `has_gaps` — 是否偵測到缺口
- `gap_count_estimate` — 估計缺口數量（基於實際時間跨度 vs 預期時間跨度）

`app/api/main.py` 新增 `GET /candles/status` endpoint。

**Admin UI：** 市場資料分頁「資料狀態」區塊，點 Refresh Status 顯示每個 symbol 的狀態，含顏色標示（綠/橙/紅）。

#### 2b. `POST /market-data/fetch` — 獨立觸發市場資料抓取

**問題：** 原本只能透過跑完整 pipeline 來抓取新 candle，無法獨立觸發。

**改動：** `app/api/main.py` 新增 `POST /market-data/fetch`：
- 接受可選的 `symbols` 列表（JSON body）
- 若未傳入，使用 scheduler 的 active symbols
- 直接呼叫 `run_market_data_job()`，不觸發 strategy/risk/execution

**Admin UI：** 市場資料分頁「抓取市場資料」區塊，可填入 symbol 後點 Fetch Now。

---

### 3. RL + Inference 整合（承接前日工作）

**問題：** RL 訓練完後結果只存在 `training_jobs`，沒有進入 model registry，無法透過 inference service 使用。

**改動：**
- `RLJobRequest` 新增 `auto_promote: bool = False` 參數
- `POST /training/rl-jobs` 訓練成功後自動呼叫 `register_model()` 將 RL agent 存入 model registry（as candidate）
- `auto_promote=True` 時自動呼叫 `promote_model()` 設為 champion
- 回傳新增 `registry_model_id` 和 `registry_status`
- Inference service 無需修改，已支援 RL（與 supervised 共用相同 sigmoid 推論邏輯）
- 新增 6 個測試（Group 5: RL registry integration）

**ML/AI 分頁：** RL Experiment 面板已加入 auto-promote checkbox。

---

## 新增測試

| 測試項目 | 數量 | 結果 |
|---|---|---|
| `get_candles_status` — 空資料庫、單 symbol、無缺口、有缺口 | 4 | ✅ |
| `GET /candles/status` API endpoint | 2 | ✅ |
| `POST /market-data/fetch` endpoint（帶 symbols / 無 symbols）| 2 | ✅ |
| RL registry 整合（register / auto_promote / candidate / 失敗 / inference / status）| 6 | ✅ |
| **全部測試** | **632** | ✅ |

---

## 已知缺口（尚未處理）

| 項目 | 說明 |
|---|---|
| Retry / backoff | `fetch_klines()` 失敗一次就讓 job failed，無重試邏輯 |
| Feature materialization 未整合進 market_data_job | 每次抓完新 candle 需手動觸發特徵更新 |
| 價格欄位存為 TEXT | `open/high/low/close` 字串，計算時需轉型 |
| Symbol 寫死在程式碼 | 新增交易對需改 `symbols.py` |
| 缺乏 DB index | (symbol, timeframe, open_time) 無獨立 index，大資料量時查詢較慢 |
| 固定 1m timeframe | 不支援 5m / 1h 等多 timeframe |

---

## 下一步方向

- **Stage 5 自動重訓管線**（已規劃於 roadmap-worksheet-zh-tw-checklist.md）
- 市場資料層 retry/backoff 強化
- Feature materialization 整合進 market_data_job

---

## 下午補充

### 4. Admin Panel UI 大幅翻新（承接前日）

**改動：**
- Symbol 選擇全面改為 **toggle pill** 多選 UI（Fetch Market Data、Feature Store 均適用）
- Fetch Market Data 面板改版為專業 UI（`.fetch-panel` / `.fetch-field` 樣式系統）
- Feature Store 面板統一與 Fetch Market Data 風格
- 介面全面**英文化**
- Data Status 看板改版（`.status-dot` / `.status-badge` / `.status-stat` 統計列）
- 新增 Start Date 欄位含快速 preset（7d / 30d / 90d），預設 7 天前
- 移除多餘「補齊歷史」按鈕（已有 1000 preset）
- Fetch Now 後自動觸發 Status 更新
- Auto-refresh 保留 toggle pill 選擇狀態（`prevSelected` Set 模式）

**Bug 修復：**
- `ReferenceError: handleAction is not defined` → 改為 `runAction`
- Status board 未同步更新 → 抽出 `refreshMarketStatus()` 獨立呼叫
- Materialize Features 按鈕無反應 → generic loop 排除 `market-*` action
- Auto-refresh 清除 pill 選擇 → `prevSelected` Set 在 rebuild 前後保留狀態

**檔案：** `app/admin/page.py`

---

### 5. Binance K 線 Pagination（>1000 根）

**問題：** Binance 單次最多回傳 1000 根，無法一次抓取長時間歷史資料。

**改動：**
- `app/data/binance_client.py` — `fetch_klines()` 加入 `start_ms: Optional[int]` 參數，傳入 Binance `startTime`
- `app/pipeline/market_data_job.py` — 新增 `_fetch_all_since()` pagination loop，每次移動 cursor 至 `last_open_time + 1ms`，直到回傳筆數 < PAGE_SIZE 為止
- `app/api/main.py` — `MarketDataFetchRequest` 加入 `start_date: Optional[str]`，轉換為 `start_ms` 毫秒

---

### 6. 資料庫全面強化（Migrations 030–038）

| Migration | 內容 |
|---|---|
| 030 | candles REAL → NUMERIC(20,8)（SQLite rebuild / PG ALTER COLUMN）|
| 031 | positions/orders/fills/pnl 財務欄位 REAL → NUMERIC(20,8)（PG）|
| 032 | `idx_candles_symbol_timeframe` 索引 |
| 033 | key tables `created_at` TEXT → TIMESTAMPTZ（PG）|
| 034 | `feature_vectors.open_time` INTEGER → BIGINT（PG）|
| 035 | `updated_at` / `last_seen_at` TEXT → TIMESTAMPTZ（PG）|
| 036 | candles triple composite index、orders/fills/audit_events created_at index |
| 037 | risk_configs/portfolio_config/backtest_runs REAL → NUMERIC（PG）|
| 038 | retention 支援索引（heartbeats.component, audit_events source+created_at）|

**其他 DB 改動：**
- `app/core/db.py` — `PRAGMA foreign_keys = ON` 啟用 SQLite FK 強制執行
- `app/data/candles_service.py` — `save_klines()` 寫入時強制 `float()`/`int()` 型別轉換

**Bug 修復：**
- Migration 038 `no such column: actor` → 改為 `source` 欄位

---

### 7. Data Retention 功能（完整實作）

**新增模組：**
- `app/system/retention.py` — `purge_old_audit_events(days=90)` / `purge_completed_job_queue(days=30)` / `run_retention()`
- `app/api/main.py` — `RetentionRequest` Pydantic model、`POST /maintenance/retention` endpoint

**Admin Panel：**
- Diagnostics tab 新增 Data Retention 面板
- audit days / job queue days 各有 preset 按鈕
- Run Retention 按鈕 POST `/maintenance/retention`，顯示刪除筆數結果
- `retention-*` action 排除於 generic runAction loop 之外

---

### 8. 資料庫待改進盤點

| 優先級 | 項目 |
|---|---|
| 高 | SQLite WAL 模式（`PRAGMA journal_mode = WAL`）|
| 高 | SQLite busy timeout（`PRAGMA busy_timeout = 5000`）|
| 中 | CHECK 約束（qty/price 不允許負數）|
| 中 | Partial index on orders（僅索引開放中訂單）|
| 中 | `GET /db/stats` 各表筆數 + DB 大小端點 |
| 低 | Retention 後自動 VACUUM（SQLite）/ ANALYZE（PG）|

---

---

## 傍晚補充

### 9. Execution Controls UI 重構（`app/admin/page.py`）

**改動：**
- `<select multiple>` 全部替換為 **toggle pill** 多選 UI：
  - `pipeline-symbol-pills`、`scheduler-strategy-pills`、`scheduler-disabled-strategy-pills`、`scheduler-symbol-pills`
- Controls 改為 **2欄佈局**（Pipeline + Kill Switch 左欄，Scheduler 右欄跨兩行）
- Kill Switch 新增狀態 badge（`kill-switch-panel-badge`），即時顯示啟用/停用狀態
- Pipeline 結果從 raw JSON 改為：
  - 頂部 badge（OK/ERROR + 顏色）
  - `<details>` 展開 Raw JSON，含語法高亮（`.json-key` / `.json-str` / `.json-num` / `.json-bool` / `.json-null`）
- `renderPipelineResult(result)` — 獨立 renderer 函式，含 inner try/catch
- `syntaxHighlightJson(value)` — HTML escape + regex 上色

**Bug 修復：**
- 按鈕文字更新以對齊測試：`'Reset priorities'` → `'Reset'`、`'Clear notes'` → `'Clear Notes'`、`'Apply top-1'`/`'All enabled'` → `'top-1'`/`'all'`

---

### 10. Refresh Data 按鈕回饋修復

**問題：** 點擊 Refresh Data 按鈕後無任何視覺反應。

**改動：** `data-refresh` 按鈕加入 loading 狀態：
- 點擊後 disabled + 文字改為 "Refreshing..."
- `refreshAll()` 完成後恢復原始文字

---

### 11. Candles 表格：Fetch Now 後顯示最新資料

**問題：** Fetch Market Data 之後看不到實際抓取的 K 線資料。

**改動：**
- `GET /candles` endpoint 新增 `symbol` query 參數（`app/api/main.py`）
- `get_candles()` 支援 WHERE symbol 過濾（`app/query/read_service.py`）
- `SELECT_CANDLES_SQL` 補全所有 Binance K 線欄位（`quote_asset_volume`、`number_of_trades`、`taker_buy_base_volume`、`taker_buy_quote_volume`）
- Fetch Now 後自動抓取每個 symbol 的最新 10 筆，以全寬獨立面板顯示（`market-candles-panel`）
- 欄位名稱縮短顯示：`quote_vol`、`trades`、`taker_base`、`taker_quote`
- 移除 `close_time` 欄位（資訊冗餘）

---

### 12. Candles 表格視覺化升級

**改動：**（`app/admin/page.py` CSS + JS）
- 標題列改為 `.candles-table-header`：symbol 用藍色 badge、旁邊 "Last N candles" 標籤

---

## 夜間補充

### 13. Features Tab 拆分與資訊架構調整

**改動：**
- Admin Navbar 新增 **Features** 分頁
- 原本放在 Market Data 的 **Feature Store** 操作區塊移到獨立 Features tab
- Market Data 分頁收斂為 candle fetch / data status
- ML / AI 分頁文案調整，避免與 Feature Store 職責重疊

**目的：**
- 為後續 feature engineering 預留獨立工作區
- 將資料抓取、特徵工程、模型操作三層分開

**檔案：** `app/admin/page.py`

---

### 14. Scheduler / Runtime 恢復排查與修復

**排查結果：**
- 確認當時 runtime 同時被 `runtime/scheduler.stop` 與 `runtime/kill.switch` 卡住
- 清除兩個控制旗標後，發現真正阻塞點是 **launch agent 未載入**
- 進一步驗證 `scripts/run_scheduler.py --iterations 1` 可正常執行，表示 runtime 本身可跑
- 重新執行 `scripts/install_launch_agent.py` 後，launch agent 恢復為 `state = running`

**恢復結果：**
- Scheduler process 恢復
- `scheduler.log` 重新出現新的 pipeline loop
- soak validation 從 stale/degraded 回到 `status=ok`

**備註：**
- 先前 `launchd.stderr.log` 中的 `certifi` / CA bundle 錯誤為舊啟動殘留訊息，並非目前 `.venv` 的持續性故障

---

### 15. Broker Protection 語意收斂

**問題：**
- `broker_protection` 原本只要最新 3 筆 `risk_events` 都是 `REJECTED` 就會 degraded
- 對 `momentum_3bar` + `cooldown=300s` 這類策略，會把**正常風控拒單**誤判成 broker protection 問題

**第一步調整：**
- 將以下「預期內拒單」從 `risk_reject_streak` 判定排除：
  - `Cooldown active`
  - `Signal is HOLD.`
  - `Duplicate signal type.`

**第二步調整：**
- backend payload 新增：
  - `expected_rejected_risk_streak`
  - `expected_latest_rejection_reason`
  - `anomalous_rejected_risk_streak`
- 讓監控可明確分辨「正常拒單累積」與「異常拒單累積」

**保留 degraded 的類型：**
- 非 cooldown / hold / duplicate 的連續拒單仍會觸發 `risk_reject_streak`
- 例如：
  - `Existing long position already open`
  - `No position available to sell`
  - `Daily loss limit breached`

**檔案：**
- `app/api/main.py`
- `tests/test_trading_flow.py`

---

### 16. Admin / Telegram 告警文案同步

**Admin UI：**
- broker protection 顯示改為更可讀的：
  - `expected streak=...`
  - `anomalous streak=...`
- `expected streak` 採低權重淡色 pill
- `anomalous streak` 採較醒目的警示 pill

**Telegram Broker Alert：**
- 若存在 `anomalous_rejected_risk_streak`，優先使用這個欄位
- `expected_rejected_risk_streak` 與 `expected_latest_rejection_reason` 只作背景資訊附帶
- 避免把正常 reject streak 和真正異常混成同一種訊號

**檔案：**
- `app/admin/page.py`
- `app/alerting/broker.py`
- `tests/test_trading_flow.py`

---

### 17. 驗證結果

**測試：**
- `./.venv/bin/pytest tests/test_trading_flow.py -k "broker_protection_check"` → `9 passed`
- `./.venv/bin/pytest tests/test_trading_flow.py -k "maybe_send_broker_alert or broker_protection_check"` → `14 passed`

**實際運行觀察：**
- scheduler 恢復後，BTCUSDT `momentum_3bar` 重新開始產生 signal / risk / execution
- 最新一輪在 `SELL FILLED` 後，連續 `SELL REJECTED` 主要原因仍是 `Cooldown active`
- 調整後，這類預期內 reject streak 不再把 broker protection 誤打成主要異常
- 表頭深色背景、全大寫小字（11px monospace）
- **漲跌著色：**
  - close > open → 整行淡綠色底（`.bull-row`）、close 欄位綠色粗體 + ▲（`.cell-bull`）
  - close < open → 整行淡紅色底（`.bear-row`）、close 欄位紅色粗體 + ▼（`.cell-bear`）
- Hover 效果（淡藍色高亮）
- 整表 monospace 字型、圓角卡片邊框包覆

---

### 13. 面板空白修復

**問題：** Data Status 和 Fetch Market Data 面板底部有大量多餘空白。

**根因：** `.message` CSS 有 `min-height: 120px`，且 `market-fetch-message` div 預設可見（空內容）。

**改動：**
- 移除 `.message { min-height: 120px; }`
- `market-fetch-message` 加 `style="display:none"`（預設隱藏）
- 錯誤發生時 JS 設定 `msg.style.display = "block"` 再顯示

---

## 測試狀態（日終）

```
280 passed in 3.96s
```
