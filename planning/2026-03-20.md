# 2026-03-20 工作日誌

## 今日重點

- 檢查目前 SQLite runtime 中的 `daily_realized_pnl` 是否已可作為正式 daily ledger 使用
- 用既有 `fills` 與 `daily_realized_pnl` 對帳，確認最近 2 到 3 天資料是否可拿來做 Stage 1 驗證佐證

## 驗證範圍

- DB：`storage/market_data.db`
- 相關表：
  - `fills`
  - `daily_realized_pnl`
  - `risk_events`
- 相關實作：
  - `app/portfolio/daily_pnl_service.py`
  - `app/risk/risk_service.py`

## 驗證結果

### 1. 正式 daily ledger 已存在且已接入風控

- `daily_realized_pnl` 表已存在於目前 runtime DB
- `app/portfolio/daily_pnl_service.py` 已能從 `fills` 重建 `daily_realized_pnl`
- `app/risk/risk_service.py` 已透過 `get_daily_realized_pnl(...)` 讀取當日 realized PnL
- 代表 `CRYPTO_MAX_DAILY_LOSS` 目前不是只靠保守版累積值，而是已經走按日切分邏輯

### 2. 現有資料可用於 daily ledger 驗證

目前 DB 內可用的 daily ledger 資料如下：

- `2026-03-17` `BTCUSDT` `1.15408`
- `2026-03-18` `BTCUSDT` `-2.6688`
- `2026-03-19` `BTCUSDT` `-2.94313000000002`
- `2026-03-20` `BTCUSDT` `0.11437000000002454`

對應原始資料量：

- `fills`
  - `2026-03-17` `5`
  - `2026-03-18` `129`
  - `2026-03-19` `80`
  - `2026-03-20` `37`
- `risk_events`
  - `2026-03-17` `12`
  - `2026-03-18` `1159`
  - `2026-03-19` `712`
  - `2026-03-20` `330`

### 3. 以 fills 重算後，ledger 與實際成交資料一致

我用 `fills` 依時間序重算 realized PnL，結果如下：

- `2026-03-17` `BTCUSDT` `1.15408000000000`
- `2026-03-18` `BTCUSDT` `-2.66880000000000`
- `2026-03-19` `BTCUSDT` `-2.94313000000002`
- `2026-03-20` `BTCUSDT` `0.11437000000002`

與 `daily_realized_pnl` 對帳後一致。

補充：

- 檢查過程中曾看到 `2026-03-20` 的 persisted row 是舊值 `0.312160000000033`
- 但在呼叫 `get_daily_realized_pnl(...)` 後，table 已 rebuild 並更新為 `0.11437000000002454`
- 這表示 daily ledger 邏輯本身是對的，但 persisted table 可能在最近 fills 寫入後尚未被重新計算，直到被讀取或風控評估時才刷新

## 結論

- 現有這批 2 到 3 天資料可以用來做 `daily ledger` 功能驗證
- 它足以證明目前系統已具備按日切分 realized PnL 的正式邏輯
- 它也足以作為 Stage 1 風控引擎的部分驗證依據
- 但它還不能單獨取代「一週 paper trading / soak validation」的完整驗收

## 建議下一步

1. 將 checklist 中「正式 daily ledger 尚未完成」的描述改為已實作、已驗證
2. 若要讓 persisted ledger 更直觀，考慮在 fill 寫入後主動 refresh `daily_realized_pnl`
3. 繼續累積 soak / paper trading 紀錄，把 Stage 1 的驗收缺口收尾
